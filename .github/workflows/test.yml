name: CI/CD 
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run Ruff
        uses: astral-sh/ruff-action@v3
      
      - name: Run Tests
        run: python -m pytest src/ -v
